Platform Buddy v150 DIFF

Changed files:
- index.html
- styles.css
- video.js
- sw.js
- manifest.webmanifest

index.html:
- Updated PWA query versions from v149 to v150.
- Renamed the LOG video section from "Video Form Note / 動画フォームノート" to "VBT Studio / 動画フォーム/VBTノート".
- Shortened the section note to "動画でフォームと速度を残そう。"
- Renamed "動画を追加" to "VBT動画を追加".
- Renamed the submit button to "保存してVBT確認" so the upload flow leads naturally into the VBT viewer.

styles.css:
- Rebuilt `.vbt-video-frame` so the video is a normal visible block and the canvas overlay is absolutely positioned above it.
- Added min-height and max-height constraints for smartphone video display.
- Kept the canvas non-interactive by default and active only during point picking.
- Added `.vbt-video-status` for video loading/error messages.
- Added `.vbt-trim-card` and `.vbt-trim-actions` for analysis range controls.

video.js:
- Added `mediaStatusMessage()` for video loading, ready, error, and trim feedback.
- Renamed the VBT card heading to "VBT Studio β".
- Added analysis range controls:
  - "ここを開始"
  - "ここを終了"
  - trim start/end display chips
- Added `trimStart` and `trimEnd` to the in-dialog VBT state.
- Added `setVbtTrimPoint()` to set trim points from the current video time.
- Saved trim metadata inside `record.analysis.velocityData.measurement.trim`.
- Added video load/error status output below the video frame.
- After uploading and saving a video, the library opens and the VBT viewer opens automatically.
- Added click handling for `[data-vbt-trim]`.
- Did not change `app.js`.
- Did not use `localStorage`.
- Did not touch `platform-pr-v3`.

sw.js:
- Updated cache name to `platform-buddy-v150`.
- Updated cached versioned assets to v150.

manifest.webmanifest:
- Updated `start_url` to `./index.html?v=150`.

Validation:
- `node --check video.js` passed.
- `node --check app.js` passed.
- `node --check sw.js` passed.
- `app.js` SHA256 matches v149.
- No MediaPipe / Motion Analyzer strings remain.
- `video.js` contains no `localStorage` or `platform-pr-v3`.
